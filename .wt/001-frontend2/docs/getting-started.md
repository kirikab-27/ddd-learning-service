# Getting Started

Claude Multi-Agent Framework へようこそ！このガイドでは、フレームワークの導入から最初のタスク実行までを説明します。

---

## 📚 目次

1. [はじめに](#はじめに)
2. [前提条件](#前提条件)
3. [インストール](#インストール)
4. [クイックスタート](#クイックスタート)
5. [最初のタスク作成](#最初のタスク作成)
6. [Boss/Workers起動方法](#bossworkers起動方法)
7. [次のステップ](#次のステップ)

---

## はじめに

Claude Multi-Agent Frameworkは、President → Boss → Workers のヒエラルキー構造により、複雑なタスクを効率的に実行するフレームワークです。

### 主な特徴

- ✅ **Tier構造**: 依存関係を明確に管理
- ✅ **DoD 100%**: 全Workerが完了基準を達成
- ✅ **Protocol Improvement 3**: 指示書必読による高い成功率（11連続成功実績）
- ✅ **テンプレートベース**: 再利用可能な指示書・報告書

### 成功事例

- **BookRAG Manager**: 6 Workers, 5058行追加, DoD 100%, 11連続成功

### なぜこのフレームワークを使うのか

従来の単一AI開発では以下の問題がありました:

- タスクが複雑になると管理が困難
- 並列処理ができない
- 依存関係の管理が難しい
- 品質保証が不確実

このフレームワークは、これらの問題を解決します:

- **Tier構造**: タスクを依存関係に基づいて整理
- **並列実行**: 複数のWorkerが同時に作業
- **DoD**: 明確な完了基準で品質保証
- **Protocol**: ベストプラクティスに基づいた実行

---

## 前提条件

### 必須環境

1. **tmux**: Agent間通信に使用
   ```bash
   # Ubuntu/Debian
   sudo apt install tmux

   # macOS
   brew install tmux

   # 確認
   tmux -V
   # tmux 3.0a 以上推奨
   ```

2. **Claude Code**: AI Agent実行環境
   - https://claude.com/claude-code
   - Claude Code CLIのインストールが必要

3. **Git**: バージョン管理
   ```bash
   git --version
   # git version 2.25.0 以上推奨
   ```

### 推奨環境

- Linux/macOS（WSL2も可）
- Bash 4.0+
- ディスク空き容量: 1GB以上

### ソフトウェアスキル

- Gitの基本操作（commit, branch）
- ターミナルの基本操作
- tmuxの基本操作（推奨）

---

## インストール

### ステップ1: フレームワークの取得

```bash
# Clone (for contributors)
git clone https://github.com/[USERNAME]/claude-multiagent-framework.git
cd claude-multiagent-framework

# Or download ZIP and extract
curl -L https://github.com/[USERNAME]/claude-multiagent-framework/archive/refs/heads/main.zip -o framework.zip
unzip framework.zip
cd claude-multiagent-framework-main
```

### ステップ2: プロジェクトへのセットアップ

```bash
# Your project directory
cd /path/to/your/project

# Run setup script
/path/to/claude-multiagent-framework/scripts/setup-project.sh $(pwd)
```

**setup-project.sh の実行内容**:
- `instructions/` ディレクトリ作成
- `scripts/` ディレクトリ作成
- テンプレートファイルのコピー
- agent-send.sh の配置

### ステップ3: セットアップ確認

```bash
# Directory structure
ls -la instructions/
#   boss/
#   worker/
#   task/
#   planning/
#   reports/

# Check scripts
ls -la scripts/
#   agent-send.sh
#   validate-protocol.sh
#   setup-project.sh

# Validate protocol compliance
./scripts/validate-protocol.sh
```

**期待される出力**:
```
✅ Protocol Improvement 3 compliant
✅ All required directories exist
✅ Templates are in place
```

---

## クイックスタート

5ステップでタスク実行を開始できます。

### Step 1: Boss指示書の編集

`instructions/boss/boss-instructions.md` を開き、プレースホルダーを置換：

```markdown
[PROJECT_NAME] → Your Project Name
[TECH_STACK] → Next.js, TypeScript, etc.
[REPO_PATH] → /path/to/your/project
[MAIN_BRANCH] → main
```

**例**:
```markdown
# BookRAG Manager - Boss1指示書

**プロジェクト**: BookRAG Manager
**技術スタック**: Next.js 14, TypeScript, IndexedDB
**リポジトリ**: /home/user/projects/bookrag-manager
**メインブランチ**: main
```

### Step 2: タスクの作成

`instructions/task/task-001.md` を作成：

```markdown
# チケット001: 初めてのタスク

**Priority**: High
**Estimated Workers**: 2 Workers

## 目的

プロジェクトに新機能を追加する。

## Worker割り当て

| Worker | 担当タスク | 依存関係 |
|--------|-----------|---------|
| Worker1 | バックエンド実装 | なし |
| Worker2 | フロントエンド実装 | Worker1完了後 |

## DoD

- [ ] バックエンド実装完了
- [ ] フロントエンド実装完了
- [ ] TypeScript エラー: 0件
- [ ] テスト成功
```

### Step 3: tmuxセッションの起動

```bash
# Create tmux session
tmux new -s agents-myproject

# Split panes (example layout)
# Ctrl+b, % (vertical split)
# Ctrl+b, " (horizontal split)

# Pane layout:
# ┌─────────────┬─────────────┐
# │  President  │    Boss1    │
# │   (pane 0)  │  (pane 1)   │
# ├─────────────┼─────────────┤
# │  Worker1    │   Worker2   │
# │  (pane 2)   │  (pane 3)   │
# └─────────────┴─────────────┘
```

**tmux 分割コマンド**:
```bash
# Window 0で開始
# Ctrl+b, % (縦分割 → Boss1)
# Ctrl+b, " (横分割 → Worker1)
# Ctrl+b, → (右ペインに移動)
# Ctrl+b, " (横分割 → Worker2)
```

### Step 4: Agentの起動

各tmux paneで：

```bash
# Pane 0 (President)
cd /path/to/your/project
# (Wait for tasks, monitor progress)

# Pane 1 (Boss1)
cd /path/to/your/project
claude-code  # または Claude Code CLI起動

# Pane 2-3 (Workers)
cd /path/to/your/project
claude-code  # 各Workerで起動
```

**Claude Code起動時の設定**:
- 各ペインで独立したセッション
- プロジェクトルートをワーキングディレクトリに設定

### Step 5: タスクの送信

```bash
# From President pane (または任意のペイン)
./scripts/agent-send.sh boss1 "instructions/task/task-001.md を読んで実行してください"
```

Boss1がタスクを受け取り、Worker1, Worker2に指示を送信します。

**実行フロー**:
```
President → Boss1: "task-001.md を実行"
Boss1 → Worker1: "バックエンド実装を開始"
Worker1 → Boss1: "完了報告"
Boss1 → Worker2: "フロントエンド実装を開始"
Worker2 → Boss1: "完了報告"
Boss1 → President: "全タスク完了"
```

---

## 最初のタスク作成

詳細なタスク作成ガイド：

### タスクテンプレートの使用

```bash
# Copy template
cp instructions/task/task-template.md instructions/task/task-001.md

# Edit with your favorite editor
vim instructions/task/task-001.md
```

### プレースホルダーの置換

**置換リスト**:
- `[TICKET_NUMBER]`: 001
- `[TASK_NAME]`: 機能名（例: ユーザー認証機能）
- `[NUM_WORKERS]`: Worker数（例: 3）
- `[TIER_STRUCTURE]`: 依存関係（例: Tier1: Worker1, Tier2: Worker2-3）

**例**:
```markdown
# チケット001: ユーザー認証機能

**Priority**: High
**Estimated Workers**: 3 Workers

## Tier構造

- **Tier1 (並列実行可能)**: Worker1（認証API）
- **Tier2 (Worker1完了後)**: Worker2（ログインUI）, Worker3（テスト）
```

### DoD（Definition of Done）の定義

明確な完了基準を記載：

```markdown
## DoD

- [ ] 実装完了
  - [ ] 認証APIエンドポイント実装
  - [ ] ログインUIコンポーネント実装
- [ ] TypeScriptエラー: 0件
  - [ ] npx tsc --noEmit で確認
- [ ] テスト成功（Jest: 100%カバレッジ）
- [ ] ドキュメント更新
  - [ ] API仕様書
  - [ ] ユーザーガイド
```

**DoD作成のコツ**:
- 具体的で測定可能な基準
- チェックボックス形式で進捗を可視化
- 技術的な基準（エラー0件、テスト成功）を含める

### Worker別指示書の作成

各Workerに詳細な指示を提供：

```bash
# Worker1用指示書
vim instructions/worker/task-001-worker1.md
```

**Worker指示書テンプレート**:
```markdown
# チケット001 ジョブA: 認証API実装

**Worker**: Worker1
**Task**: 認証APIエンドポイント実装

## DoD
- [ ] POST /api/auth/login 実装
- [ ] JWT トークン生成
- [ ] TypeScript エラー: 0件

## 実装ガイドライン
[詳細な実装手順...]
```

---

## Boss/Workers起動方法

### tmux レイアウト例

#### 小規模プロジェクト（2-3 Workers）

```
┌─────────────┬─────────────┐
│  President  │    Boss1    │
│   (pane 0)  │  (pane 1)   │
├─────────────┼─────────────┤
│  Worker1    │   Worker2   │
│  (pane 2)   │  (pane 3)   │
└─────────────┴─────────────┘
```

#### 中規模プロジェクト（4-6 Workers）

```
┌─────────────┬─────────────┐
│  President  │    Boss1    │
│   (pane 0)  │  (pane 1)   │
├─────────────┼─────────────┤
│  Worker1    │   Worker2   │
│  (pane 2)   │  (pane 3)   │
├─────────────┼─────────────┤
│  Worker3    │   Worker4   │
│  (pane 4)   │  (pane 5)   │
└─────────────┴─────────────┘
```

### tmux基本操作

**ペイン分割**:
- `Ctrl+b, %`: 縦分割（左右）
- `Ctrl+b, "`: 横分割（上下）

**ペイン移動**:
- `Ctrl+b, ↑↓←→`: 矢印キーでペイン移動
- `Ctrl+b, o`: 次のペインに移動

**セッション管理**:
- `Ctrl+b, d`: デタッチ（セッションを終了せずに離脱）
- `tmux attach -t agents-myproject`: 再接続
- `tmux ls`: セッション一覧表示

**ペインサイズ調整**:
- `Ctrl+b, :resize-pane -D 5`: 下に5行拡大
- `Ctrl+b, :resize-pane -U 5`: 上に5行拡大

### agent-send.shの使い方

**基本構文**:
```bash
./scripts/agent-send.sh <agent-name> "<message>"
```

**例**:
```bash
# Boss1に指示
./scripts/agent-send.sh boss1 "task-001.md を実行してください"

# Worker1に直接指示（緊急時のみ）
./scripts/agent-send.sh worker1 "エラーログを確認してください"

# Worker2に完了確認
./scripts/agent-send.sh worker2 "進捗状況を報告してください"
```

**agent-send.sh の仕組み**:
- tmux の `send-keys` コマンドを使用
- 指定したペインにメッセージを送信
- ログは `logs/send_log.txt` に記録

---

## 次のステップ

### 学習リソース

1. **Best Practices**: [docs/best-practices.md](./best-practices.md)
   - Protocol Improvement 3のベストプラクティス
   - タスク分割の原則
   - DoD作成のコツ

2. **Troubleshooting**: [docs/troubleshooting.md](./troubleshooting.md)
   - よくある問題と解決方法
   - デバッグ方法
   - パフォーマンス最適化

3. **Case Study**: [docs/case-studies/bookrag-manager.md](./case-studies/bookrag-manager.md)
   - BookRAG Managerの成功事例
   - 11連続成功の秘訣
   - 具体的な実装例

### 実践プロジェクト

**チュートリアル: Todo アプリ構築**

1. **タスク作成**: 簡単なTodoアプリのタスクを作成
2. **Worker割り当て**: 2-3 Workersで実装
3. **実行**: tmuxでAgentを起動して実行
4. **評価**: DoD 100%達成を目指す

**推奨ステップ**:
1. バックエンドAPI実装（Worker1）
2. フロントエンドUI実装（Worker2）
3. テスト作成（Worker3）

### コミュニティ

- **GitHub Issues**: バグ報告や機能リクエスト
- **Discussions**: 質問や議論
- **Examples**: サンプルプロジェクト集

---

## サポート

### ドキュメント

- **API Reference**: 詳細なAPI仕様
- **Architecture**: フレームワークアーキテクチャ
- **Contributing**: 貢献ガイド

### コミュニケーション

- **Issues**: https://github.com/[USERNAME]/claude-multiagent-framework/issues
- **Discussions**: https://github.com/[USERNAME]/claude-multiagent-framework/discussions
- **Email**: support@example.com

### FAQ

**Q: Worker数の制限はありますか？**
A: 推奨は1-6 Workersです。それ以上は管理が複雑になります。

**Q: tmuxは必須ですか？**
A: はい、Agent間通信に必要です。代替手段は現在ありません。

**Q: Windowsで動作しますか？**
A: WSL2経由で動作します。ネイティブWindowsは未サポートです。

**Q: 既存プロジェクトに導入できますか？**
A: はい、setup-project.shで既存プロジェクトにセットアップできます。

**Q: 有料ですか？**
A: フレームワーク自体はオープンソースです。Claude Code APIの利用料は別途かかります。

---

**次のステップ**: [Best Practices](./best-practices.md) を読んで、フレームワークの効果的な使い方を学びましょう。
